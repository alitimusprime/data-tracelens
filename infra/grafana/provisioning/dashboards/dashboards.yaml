apiVersion: 1

providers:
  - name: TraceLens
    orgId: 1
    folder: TraceLens Raw Telemetry
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
